# JavaScript rule routing

These rules apply to browser JavaScript:

- `language.md`: JavaScript modules, browser APIs, errors, and DOM behavior
- `stimulus.md`: Stimulus controller structure and lifecycle
- `tooling.md`: ESLint and JavaScript validation

Turbo rendering rules remain in `ruby-on-rails/views.md` because this project
uses Turbo through Rails. Interface behavior remains in `frontend/`.
